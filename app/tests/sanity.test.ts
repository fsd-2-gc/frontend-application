import { describe, it, expect } from 'vitest';

// A simple sanity test so the project has at least one passing test

describe('sanity', () => {
  it('returns true', () => {
    expect(true).toBe(true);
  });
});
